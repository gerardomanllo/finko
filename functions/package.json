{
  "name": "finko-functions",
  "private": true,
  "engines": {
    "node": "24"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "lint": "eslint --ext .ts src/",
    "test": "jest",
    "test:ci": "npm run build && npm test",
    "migrate:liability-balances": "npm run build && node lib/tools/migrateLiabilityBalances.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^7.2.5",
    "luxon": "^3.5.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/luxon": "^3.4.2",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.9",
    "typescript": "^5.7.2"
  }
}
